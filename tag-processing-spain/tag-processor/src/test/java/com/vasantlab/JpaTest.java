package com.vasantlab;

/*@RunWith(SpringRunner.class)
@SpringBootTest(properties = "application1.properties")
@EnableJpaRepositories(basePackages = "org.vasantlab.dao.interfaces")
@EnableTransactionManagement*/
public class JpaTest {

	// @Autowired
	// private OrdinaryDataRFDetailsDao ordinaryDataRFDetailsDao;

	/*
	 * @Test public void testSaveDate() { OrdinaryDataRFDetails ord = new
	 * OrdinaryDataRFDetails(); ord.setBarCode("11111");
	 * ord.setNo_try_writetag(0); ord.setOrderId("1000");
	 * ord.setRfDetails("111113333fff"); ord.setStatus("RE");
	 * ord.setCreate_date(LocalDate.now().format(DateTimeFormatter.ofPattern(
	 * "dd/MM/yyyy")));
	 * ord.setModify_date(LocalDate.now().format(DateTimeFormatter.ofPattern(
	 * "dd/MM/yyyy"))); ordinaryDataRFDetailsDao.saveAndFlush(ord);
	 * System.out.println(ord.getCreate_date());
	 * Assert.assertNotNull(ord.getCreate_date()); }
	 */
}
