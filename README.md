# tagprocessing
The tag processing automation allow a large no of RFID tags to be processed using <b>Sargas driver</b>. The project was designed to interface with a com device, which allow the tags to be moved between the read and write antenna and then complete the process of writing and verifying EPC data and other parameters that RFID tag support. 

